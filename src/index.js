import { StrictMode } from "react";
import ReactDOM from "react-dom";
import FireStoreTuts from "./FireStoreTuts";

// import App from "./App";

// if you want to know sign up and sigin   flow please insert App

const rootElement = document.getElementById("root");
ReactDOM.render(
  <StrictMode>
    <FireStoreTuts />
  </StrictMode>,
  rootElement
);
